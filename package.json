{
  "name": "telegram-msg-count-bot",
  "version": "1.0.0",
  "description": "A telegram bot for counting msg",
  "main": "handler.js",
  "scripts": {
    "lint": "prettier --write handler.js src/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dynamo": "sls dynamodb start --migrate",
    "invoke": "serverless invoke local --function bot"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/SASUKE40/telegram-msg-count-bot.git"
  },
  "keywords": [
    "telegram",
    "telegram-bot"
  ],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/SASUKE40/telegram-msg-count-bot/issues"
  },
  "homepage": "https://github.com/SASUKE40/telegram-msg-count-bot#readme",
  "dependencies": {
    "aws-sdk": "^2.667.0",
    "serverless-dynamodb-local": "^0.2.39",
    "telegraf": "^3.38.0"
  },
  "devDependencies": {
    "prettier": "^2.0.5"
  }
}
